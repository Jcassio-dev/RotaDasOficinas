import { styled } from "styled-components";

export const Container = styled.div`
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;

    padding: 20px;
    width: 100%;

    position: absolute;
    top: 0;
    left: 0;

    button{
        font-size: 25px;

        background: none;
        border: none;

        color: #FFF;
    }
`